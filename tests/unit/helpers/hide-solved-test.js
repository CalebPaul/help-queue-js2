import { hideSolved } from 'help-queue/helpers/hide-solved';
import { module, test } from 'qunit';

module('Unit | Helper | hide solved');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hideSolved([42]);
  assert.ok(result);
});
